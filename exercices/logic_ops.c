#include <stdio.h>
#include <stdbool.h>

int main(){
    // logic operators && and

    // float temp = 25;
    // bool sunny = true;
    
    // if(temp >= 0  && temp <=30 && sunny)
    // {
    //     printf("the weather is good!");
    // }
    // else {
    //     printf("the weather is bad");
    // }

    // logic operators || OR

    // float temp = 25;
    
    
    // if(temp <= 0 || temp >=30)
    // {
    //     printf("the weather is bad!");
    // }
    // else {
    //     printf("the weather is good");
    // }

    // logic operator : ! (not)

    bool sunny = false;

    if (!sunny) {
        printf("\n it's cloudy outside");
    }
    else {
        printf("\n it's sunny outside");
    }



    return 0;
}